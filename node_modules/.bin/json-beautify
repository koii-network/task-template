#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../json-beautify/bin/json-beautify" "$@"
  ret=$?
else 
  node  "$basedir/../json-beautify/bin/json-beautify" "$@"
  ret=$?
fi
exit $ret
